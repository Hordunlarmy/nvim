# Changelog

## [Unreleased]

### Changed
- Removed the `<leader>dt` keybinding for "Git diff popup (unified)" from both `remap.lua` and `gitsigns.lua` plugins. The same keybinding is now repurposed for toggling buffer diagnostics.
- Updated Clojure LSP configuration to support single file mode and improved project cache handling with dedicated cache directories per project.

### Fixed
- Updated Clojure root pattern detection to include additional project files (`build.clj`) and improved fallback logic using git ancestor detection.